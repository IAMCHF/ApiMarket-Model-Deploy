# ============================================================================
# requirements/ocr.txt — OCR / 文档解析（23 个模型）
# ============================================================================

# --- PaddlePaddle 系（PaddleOCR-VL-1.6 / PP-OCRv5） ---
# paddlepaddle-gpu 自带 CUDA 运行时，与镜像 CUDA 共存（见方案 8.1）
paddlepaddle-gpu>=2.6.0
paddleocr>=2.8.0

# --- docling 系（granite-docling-258M） ---
docling>=2.0.0

# --- VLM OCR 共用 ---
# transformers / torch 已在镜像 L2 固化；此处仅补充视觉处理
opencv-python-headless>=4.9.0
