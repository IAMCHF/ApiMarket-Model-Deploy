# ============================================================================
# requirements/other.txt — 其他专用（7 个模型：时序预测 / 隐私过滤 / 安全检测）
# ============================================================================

# --- 时序预测（amazon/chronos 系列 + chronos-bolt 系列） ---
# 注：PyPI 最新 2.3.1（与 GitHub main 同版本）的 ChronosConfig 不支持
#     bolt/chronos-2 新 config（input_patch_size 等，2026-08 实测），
#     该系列模型需等待 Amazon 发布新版包；安装本包保证依赖完整，加载失败会有明确错误。
chronos-forecasting>=2.0.0

# --- 时序预测（google/timesfm-2.5-200m-pytorch） ---
# 注：timesfm 官方包名在 PyPI 上不固定（timesfm / timesfm-utilities 均有历史），
# 按需按官方 README 安装；当前不阻塞 chronos 系列。
# timesfm-utilities>=0.1.0

# --- TimesFM 适配器 DataFrame 处理 ---
pandas>=2.0.0

# --- 隐私过滤（openai/privacy-filter，transformers 已在镜像 L2 固化） ---

# --- 安全检测（cisco-ai/Antares-1B，需 Cisco Antares 运行时，按官方 README 补齐后追加） ---
