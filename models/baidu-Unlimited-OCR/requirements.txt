# 依赖清单：common（平台通用）+ ocr（模型类别）

# ===== common =====
# ============================================================================
# requirements/common.txt — 所有服务必装的通用依赖（entrypoint 始终安装）
# 对应 category: other 及平台基础
# ============================================================================

# --- API 服务框架 ---
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
python-multipart>=0.0.9

# --- 配置与序列化 ---
pyyaml>=6.0
requests>=2.32.0

# ===== ocr =====
# ============================================================================
# requirements/ocr.txt — OCR / 文档解析
#
# 说明：本文件为类别级依赖清单。重依赖（paddle / docling）按模型按需启用，
#       默认只装轻量依赖，避免每部署一个 OCR 模型都全量安装。
# ============================================================================

# --- 通用视觉处理 ---
opencv-python-headless>=4.9.0

# --- baidu/Unlimited-OCR 官方推理依赖（AutoModel + model.infer） ---
einops>=0.8.0
addict>=2.4.0
easydict>=1.13
pymupdf>=1.24.0
psutil>=5.9.0
matplotlib>=3.8.0

# ---------------------------------------------------------------------------
# 可选重依赖（按模型启用，部署对应模型时解除注释）：
# ---------------------------------------------------------------------------
# PaddleOCR 系（PaddlePaddle/PaddleOCR-VL-1.6、PaddlePaddle/PP-OCRv5）：
#   paddlepaddle-gpu>=2.6.0
#   paddleocr>=2.8.0
#
# docling 系（ibm-granite/granite-docling-258M）：
#   docling>=2.0.0
# ---------------------------------------------------------------------------

# ===== baidu/Unlimited-OCR 模型级附加依赖 =====
transformers==4.57.1
matplotlib>=3.8.0
