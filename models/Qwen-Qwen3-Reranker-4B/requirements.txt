# 依赖清单：common（平台通用）+ embedding（模型类别）

# ===== common =====
# ============================================================================
# requirements/common.txt — 所有服务必装的通用依赖（entrypoint 始终安装）
# 对应 category: other 及平台基础
# ============================================================================

# --- API 服务框架 ---
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
python-multipart>=0.0.9

# --- 配置与序列化 ---
pyyaml>=6.0
requests>=2.32.0

# ===== embedding =====
# ============================================================================
# requirements/embedding.txt — 嵌入检索 / 重排序（17 个模型）
# ============================================================================

# --- sentence-transformers 系（MiniLM / bge-m3 / nomic / arctic） ---
sentence-transformers>=2.7.0

# --- transformers 系嵌入（Nemotron-Embed / Qwen3-Embedding / jina / gemma / granite） ---
# transformers 已在镜像 L2 固化；Qwen3-Embedding 需要较新版本，按需覆盖
transformers>=4.50.0
