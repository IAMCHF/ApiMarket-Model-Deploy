# ============================================================================
# 基础框架依赖 — 固化在镜像 L2 层
# PyTorch 已在 Dockerfile 中通过 --index-url 单独安装，此处不重复
# ============================================================================

# --- Hugging Face 生态 ---
transformers>=4.44.0
accelerate>=0.33.0
huggingface_hub>=0.24.0
safetensors>=0.4.3
tokenizers>=0.19.0

# --- 量化推理 ---
# 0.44+ 支持 torch 2.5（2026-08 镜像升级 torch 2.5.1 后匹配）
bitsandbytes>=0.44.0

# --- ONNX 运行时（GPU） ---
onnxruntime-gpu>=1.19.0

# --- 常用基础库（transformers 的间接依赖，显式锁定版本下限） ---
numpy>=1.26.0
scipy>=1.13.0
pillow>=10.4.0
einops>=0.8.0
sentencepiece>=0.2.0
protobuf>=4.25.0
pyyaml>=6.0
requests>=2.32.0
tqdm>=4.66.0
